import React from 'react';
import { View, StyleSheet } from 'react-native';

const ServiceCenterWizard = () => {
    return (
        <View style={styles.container}>
        </View>
    );
};

export default ServiceCenterWizard;

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },

});